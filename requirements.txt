# --- [데이터 처리 및 분석] ---
pandas          # 데이터 프레임 처리 (엑셀/CSV 읽기 및 가공)
numpy           # 수치 계산 및 결측치(NaN, Inf) 처리
openpyxl        # Pandas가 엑셀 파일(.xlsx)을 읽을 때 사용하는 엔진

# --- [데이터베이스 및 환경설정] ---
supabase        # Supabase DB 연동 클라이언트
python-dotenv   # .env 파일에서 API 키 등 환경변수 로드